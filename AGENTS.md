# Study OS 開発ルール

## 変更時の記録

コードや問題データを変更した場合は、作業完了前に動作確認し、次の項目を含むGitコミットを作成する。

- 問題
- 変更前
- 判断理由
- 変更内容
- 変更後
- 確認結果
- 未解決

推測で理由を作らない。会話や作業記録から分からない項目は「不明」と記載する。

## 安全ルール

- `.env`、APIキー、認証情報、個人情報をコミットしない。
- `source_docs/`の参考資料・写真・音声はコミットしない。
- commit前に `git diff --cached` と秘密情報検査を確認する。
- pushはユーザーから明示的に依頼された場合だけ行う。
- Git履歴の書き換え、強制push、リポジトリ削除は行わない。

## 週次レポート

毎週のGit差分は `scripts/generate_weekly_kairos_report.sh` で整理する。
生成物は `reports/kairos-weekly/` に置き、人間が確認してからKAIROS / Business OSへ反映する。
